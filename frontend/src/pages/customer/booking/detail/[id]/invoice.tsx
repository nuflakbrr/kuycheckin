import { FC } from 'react';

import ContainerInvoice from '@/components/Containers/Dashboard/components/Invoice';

const Invoice: FC = () => {
  return <ContainerInvoice />;
};

export default Invoice;
